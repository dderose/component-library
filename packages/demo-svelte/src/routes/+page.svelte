<h2>Welcome</h2>

<p>
  This demo showcases each component from <code>@component-library/core</code>,
  wired up with the Svelte 5 <code>useLogic</code> adapter. Pick a component
  from the nav above to see it in action.
</p>

<p>
  Each demo uses the headless logic class directly so you can see the raw state
  updates and how the API works. In a real app you'd wrap these in styled Svelte
  components.
</p>
