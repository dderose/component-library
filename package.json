{
  "name": "component-library",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "test:core": "bun test --filter '*' --cwd packages/core",
    "typecheck": "bun run --filter '*' typecheck"
  }
}
